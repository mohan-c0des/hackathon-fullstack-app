"""from fastapi import APIRouter

router = APIRouter(
    prefix="/api",
    tags=["Base"]
)

@router.get("/")
def home():
    return {
        "status": "OK",
        "message": "Professional FastAPI backend is running"
    }"""
